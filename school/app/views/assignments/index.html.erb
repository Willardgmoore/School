<h1>Assignments</h1>
<%= link_to 'New Assignment', new_course_assignment_path %>

<% @assignments.each do |assignment| %>
  <h3><%= assignment.title %></h3>
  <%= link_to 'Assignment Details', course_assignment_path(assignment.id, assignment) %>
<% end %>
